classdef CE

    properties

    end

    methods

        function obj = CE()

        end

        function [e] = forward(~,y,yp)

            P = exp(yp)./sum(exp(yp));

            e = dot();

        end
    end
end